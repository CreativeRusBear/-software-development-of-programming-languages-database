<h1>Документация</h1>
<h2>Содержание</h2>
<a href="#structure">1. Построение структуры базы данных</a>
<a href="#interface">2. Разработка интерфейса базы данных</a>
<a href="#request">3. Разработка запросов пользователей к базе данных</a>
<a href="#security">4. Организация защиты информации, хранящейся в базе данных</a>
<a href="#model">5. Модель структуры базы данных</a>
<h2 id="structure">1. Построение структуры базы данных</h2>
<p>База данных разделена на 3 таблицы: Языки программирования, Категории, Области применения.</p>
<p><b>Таблица «Языки программирования»  содержит поля:</b></p>
<ul>
	<li><p>id_pl			идентификатор языка программирования;</p></li>
	<li><p>title			название языка программирования;</p></li>
	<li><p>history			история;</p></li>
	<li><p>logo			    логотип;</p></li>
	<li><p>create_year		дата создания;</p></li>
	<li><p>libs_frameworks	популярные библиотеки и фреймворки  этого языка.</p></li>
</ul>

<p><b>Таблица «Категория» содержит поля:</b></p>
<ul>
	<li><p>id_category	    идентификатор класса языка;</p></li>
	<li><p>category		    название класса языка.</p></li>
</ul>

<p><b>Таблица «Область применения» содержит поля:</b></p>
<ul>
	<li><p>id_use		    идентификатор области применения;</p></li>
	<li><p>use_name	        название области применения.</p></li>
</ul>
<h2 id="interface">2. Разработка интерфейса базы данных</h2>
<img src="https://user-images.githubusercontent.com/37180024/50382376-d179b000-06ae-11e9-9fe4-dfa5d27cc839.png" alt="Рисунок 1. Таблица «Язык программирования»">
<p>Рисунок 1. Таблица «Язык программирования»</p>
<br>
<img src="https://user-images.githubusercontent.com/37180024/50382389-16054b80-06af-11e9-91d4-a57ba3e04634.png" alt="Рисунок 2. Таблица «Категория»">
<p>Рисунок 2. Таблица «Категория»</p>
<br>
<img src="https://user-images.githubusercontent.com/37180024/50382395-31705680-06af-11e9-8172-37fbd3eb028c.png" alt="Рисунок 3. Таблица «Область применения»">
<p>Рисунок 3. Таблица «Область применения»</p>
<br>
<img src="https://user-images.githubusercontent.com/37180024/50382406-5fee3180-06af-11e9-913d-d2a406656bdd.png" alt="Рисунок 4. Таблица, предназначенная для хранения данных о  категориях определенного языка программирования">
<p>Рисунок 4. Таблица, предназначенная для хранения данных о  категориях определенного языка программирования</p>
<br>
<img src="https://user-images.githubusercontent.com/37180024/50382412-814f1d80-06af-11e9-951d-2182f73f8144.png" alt="Рисунок 5. Таблица, предназначенная для хранения данных об областях применения определенного языка программирования">
<p>Рисунок 5. Таблица, предназначенная для хранения данных об областях применения определенного языка программирования</p>
<br>
<p>Для работы с базой данных была написана программма на языке программирования Lazarus.</p>
<h3>Процедуры в форме «Вход»:</h3>
<p>Процедура LogBtnClick- стандартный обработчик события одинарного щелчка  левой кнопки мыши на объекте LogBtn. Используется для перехода на форму главного меню  под обычным пользователем.</p>
<p>Процедура LogBtnMouseEnter- процедура, которая отслеживает событие вхождения мыши в пределы визуального компонента (в данном случае, кнопки LogBtn). Используется для изменения размера кнопки и замены изображения на ней.</p>
<p>Процедура LogBtnMouseLeave- процедура, которая отслеживает событие выхода мыши за  пределы визуального компонента (в данном случае, кнопки LogBtn). Используется для изменения размера кнопки и замены изображения на ней (предоставляет первоначальный вид элементу).</p>
<p>Процедура FormKeyPress- стандартный обработчик события нажатия кнопки на клавиатуре. Используется для вызова процедуры LogBtn.Click. И в последующем для перехода на форму главного меню.</p>
<p>Процедура miAdminClick-стандартный обработчик события одинарного щелчка левой кнопки мыши на объект miAdmin. Используется для перехода на форму, которая предназначена для входа «под именем администратора».</p>
<p>Процедура miExitClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект miExit. Используется для выхода из программы.</p>
<h3>Процедуры в форме «Авторизация»:</h3>
<p>Процедура BackBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект BackBtn. Используется для перехода на предыдущую форму программы.</p>
<p>Процедура BackBtnMouseEnter- процедура, которая отслеживает событие вхождения мыши в пределы визуального компонента (в данном случае, кнопки BackBtn). Используется для изменения размера кнопки и замены изображения на ней.</p>
<p>Процедура BackBtnMouseLeave- процедура, которая отслеживает событие выхода мыши за  пределы визуального компонента (в данном случае, кнопки BackBtn). Используется для изменения размера кнопки и замены изображения на ней (предоставляет первоначальный вид элементу).</p>
<p>Процедура cbVisibleChange- стандартный обработчик события изменения содержимого (в данном случае, стоит галочка или нет). Используется для предоставления возможности показа или скрытия пароля. </p>
<p>Процедура eLogKeyPress- стандартный обработчик события нажатия кнопки на клавиатуре. Используется для передачи фокуса ePassword.</p>
<p>Процедура ePasswordKeyPress- стандартный обработчик события нажатия кнопки на клавиатуре. Используется для вызова процедуры AdminBtn.Click. И в последующем для перехода на форму главного меню «под именем администратора» (если логин и пароль введены верно).</p>
<p>Процедура FormShow- стандартный обработчик, который вызывается при каждом открытии формы. Используется для передачи фокуса eLog.</p>
<p>Процедура AdminBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект AdminBtn. Используется для проверки правильного ввода логина и пароля, а также при их достоверности (истинности) преходит на форму с главным меню «под именем администратора» . В инном случае выводит различные сообщения об ошибке, совершенные пользоватлем.</p>
<p>Процедура AdminBtnMouseEnter- процедура, которая отслеживает событие вхождения мыши в пределы визуального компонента (в данном случае, кнопки AdminBtn). Используется для изменения размера кнопки и замены изображения на ней.</p>
<p>Процедура AdminBtnMouseLeave- процедура, которая отслеживает событие выхода мыши за  пределы визуального компонента (в данном случае, кнопки AdminBtn). Используется для изменения размера кнопки и замены изображения на ней (предоставляет первоначальный вид элементу).</p>
<h3>Процедуры в форме «Главное меню»:</h3>
<p>Процедура  BackBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект BackBtn. Используется для возрата на форму «Вход».</p>
<p>Процедура BackBtnMouseEnter- процедура, которая отслеживает событие вхождения мыши в пределы визуального компонента (в данном случае, кнопки BackBtn). Используется для изменения размера кнопки и замены изображения на ней.</p>
<p>Процедура BackBtnMouseLeave- процедура, которая отслеживает событие выхода мыши за  пределы визуального компонента (в данном случае, кнопки BackBtn). Используется для изменения размера кнопки и замены изображения на ней (предоставляет первоначальный вид элементу).</p>
<p>Процедура FormKeyPress- стандартный обработчик события нажатия кнопки на клавиатуре. Используется для вызова процедуры BackBtn.Click. И в последующем для возврата на форму «Вход».</p>
<p>Процедура FormShow- стандартный обработчик, который вызывается при каждом открытии формы. Используется для передачи ототображения или скрытия кнопки (PrimechanieBtn), в завистимости от такого какие привелегии имеет пользователь.</p>
<p>Процедура miExcelClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект miExcel. Используется перехода на форму «Вывод в Excel».</p>
<p>Процедура miLanguagesClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект miLanguages. Используется перехода на форму «Описание».</p>
<p>Процедура PrimechanieBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект PrimechanieBtn. Используется для показа/скрытия окна с примечанием.</p>
<p>Процедура PrimechanieBtnMouseEnter- процедура, которая отслеживает событие вхождения мыши в пределы визуального компонента (в данном случае, кнопки PrimechanieBtn). Используется для изменения размера кнопки и замены изображения на ней.</p>
<p>Процедура PrimechanieBtnMouseLeave- процедура, которая отслеживает событие выхода мыши за  пределы визуального компонента (в данном случае, кнопки PrimechanieBtn). Используется для изменения размера кнопки и замены изображения на ней (предоставляет первоначальный вид элементу).</p>
<h3>Процедуры в форме «Описание»:</h3>
<p>Процедура FormShow- стандартный обработчик, который вызывается при каждом открытии формы. Используется для предоставления различных возможностей (привелегий) в зависимости от того, как вы авторизировались (администратор/ обычный пользователь).</p>
<p>Процедура ImgbtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект Imgbtn. Используется для перехода на форму редактирования изображения (логотипа).</p>
<p>Процедура msSearchTitleChange- стандартный обработчик событий любого 
изменения текста. Используется для поиска языка программирования по его названию вне зависимости от регистра. Также поиск производится по неполному названию языка программирования.</p>
<p>Процедура EditCategoryBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект EditCategoryBtn. Используется для перехода на форму «Категория».</p>
<p>Процедура FormActivate- стандартный обработчик события активации формы. Используется для отображения всех данных из таблиц «Категории» и «Области применения» в списки множественного выбора (chlbCategory, chlbApplying). Также используется для отображения выделенных элементов в списках множественного выбора.</p>
<p>Процедура EditApplyingBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект EditApplyingBtnClick. Используется для перехода на форму «Область применения».</p>
<p>Процедура AddInDBBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект EditApplyingBtnClick. Используется для добавления идентифкатора соотвестующего языка программирования и идентификаторов отмеченных записей списка категорий в базу данных «programming_languages» таблицы «FirstTable».</p>
<p>Процедура AddApplyingBtnClick- стандартный обработчик события одинарного щелчка левой кнопки мыши на объект AddApplyingBtn. Используется для добавления идентифкатора соотвестующего языка прогрмимирования и идентификаторов (id) отмеченных записей списка областей применения в базу данных «programming_languages» таблицы «SecondTable».</p>
<p>Процедура chlbApplyingItemClick- стандартный обработчик события, срабатывающий при нажатии щелчка мыши по элементу данных. Используется для выделения идентификаторов элементов в невидимом компоненте «chlbInvise».</p>
<p>Процедура chlbCategoryItemClick- стандартный обработчик события, срабатывающий при нажатии щелчка мыши по элементу данных. Используется для выделения идентификаторов элементов в невидимом компоненте «chlbInvisible».</p>
<h3><u>Процедуры в форме «Добавить или изменить логотип»:</u></h3>
<p>Процедура ObzorBtnClick- стандартный обработчик события одинарного щелчка  левой кнопки мыши на объекте ObzorBtn. Используется для выбора подходящего логотипа.</p>
<p>Процедура AddAndEditBtnClick- стандартный обработчик события одинарного щелчка  левой кнопки мыши на объекте ObzorBtn. Используется для добавления логотипа (названия и расширения) в базу данных.</p>
<p>Процедура FormShow- стандартный обработчик, который вызывается при каждом открытии формы. Используется для настройки начальных параметров формы.</p>
<h3>Процедуры в форме «Категория»:</h3>
<p>Процедура dbnCategoryBeforeAction- обработчик события, при котором код выполняется перед каким-либо действием. Используется для проверки наличия одинаковых записей.</p>
<p>Процедура FormCloseQuery- стандартный обработчик события закрытия формы. Используется для подтверждения/отклонения сохранения новой записи, которая случайным образом не была добавлена. А также добавления новых записей (если они имеются) в список множественного выбора категорий.</p>
<h3>Процедуры в форме «Область применения»:</h3>
<p>Процедура FormCloseQuery- стандартный обработчик события закрытия формы. Используется для подтверждения/отклонения сохранения новой записи, которая случайным образом не была добавлена. А также добавления новых записей (если они имеются) в список множественного выбора категорий.</p>
<p>Процедура dbnApplyingBeforeAction- обработчик события, при котором код выполняется перед каким-либо действием. Используется для проверки наличия одинаковых записей.</p>
<h3>Функции и процедуры в форме «Вывод в Excel»:</h3>
<p>Функция CheckExcelInstall- проверка на наличие установленного Excel.</p>
<p>Процедура ConclusionBtnClick- стандартный обработчик события одинарного щелчка  левой кнопки мыши на объекте ConclusionBtn. Используется для вывода выбранных данных в Excel.</p>
<p>Процедура chgTablesItemClick- стандартный обработчик события, срабатывающий при нажатии щелчка мыши по элементу данных. Используется для показа/скрытия панели группы независимых переключателей (checkbox’ов).</p>
<h3>Процедуры в форме «DM»:</h3>
<p>Процедура sqLanguagesAfterScroll-  событие, возникающее после перехода на другую запись. Используется для отображения соответствующих данных о каждом из языков программирования.</p>
<h2 id="request">3. Разработка запросов пользователей к базе данных</h2>
<p>Для создания таблицы «Язык программирования» был разработан следующий запрос:</p>
    
    CREATE TABLE programming_languages.language (
    id_pl smallint(6) NOT NULL AUTO_INCREMENT,
    title varchar(30) NOT NULL,
    history text DEFAULT NULL,
    logo varchar(200) DEFAULT 'no_photo.png',
    create_year date DEFAULT NULL,
    libs_frameworks varchar(200) DEFAULT NULL,
    PRIMARY KEY (id_pl),
    UNIQUE INDEX title (title)
    )

<p>Для создания таблицы «Категория» был разработан следующий запрос:</p>
    
    CREATE TABLE programming_languages.category (
    id_category int(11) NOT NULL AUTO_INCREMENT,
    category varchar(80) NOT NULL,
    PRIMARY KEY (id_category),
    UNIQUE INDEX category (category)
    )

<p>Для создания таблицы «Область применения» был разработан следующий запрос:</p>
   
    CREATE TABLE programming_languages.applying (
    id_use int(11) NOT NULL AUTO_INCREMENT,
    use_name varchar(75) NOT NULL,
    PRIMARY KEY (id_use),
    UNIQUE INDEX use_name (use_name)
    )

<p>Для создания таблицы «Хранение данных о  категориях определенного языка программирования» был разработан следующий запрос:</p>

    CREATE TABLE programming_languages.firsttable (
    id_pl smallint(6) DEFAULT NULL,
    id_category int(11) DEFAULT NULL
    )

<p>Для создания таблицы «Хранение данных об областях применения определенного языка программирования» был разработан следующий запрос:</p>

    CREATE TABLE programming_languages.secondtable (
    id_pl smallint(6) DEFAULT NULL,
    id_use int(11) DEFAULT NULL
    )

<br>
<p>Для доступа к базе данных были разработаны следующие запросы:</p>
<ul>
	<li>
		<pre>SELECT * FROM LANGUAGE</pre>
		<p>выбирает все поля из таблицы «Языки программирования»</p>
	</li>
	<li>
		<pre>SELECT * FROM CATEGORY</pre>
		<p>выбирает все поля из таблицы «Категория»</p>
	</li>
	<li>
		<pre>SELECT * FROM APPLYING</pre>
		<p>выбирает все поля из таблицы «Область применения»</p>
	</li>
	<li>
		<pre>SELECT * FROM FIRSTTABLE</pre>
		<p>выбирает все поля из таблицы «хранение данных о категориях определенного языка программирования»</p>
	</li>
	<li>
		<pre>SELECT * FROM SECONDTABLE</pre>
		<p>выбирает все поля из таблицы «хранение данных об областях применения определенного языка программирования»</p>
	</li>
</ul>
<h2 id="security">4. Организация защиты информации хранящийся в базе данных</h2>
<p>Защита информации хранящейся в базе данных организуется средствами MySQLServer. Доступ к базе данных может получить только 1 пользователь . Login – root, passwort – 090203. Кроме того, вход в программу под именем администратора производится с помощью логина-admin и пароля-090203.</p>
<h2 id="model">5. Модель структуры базы данных </h2>
<img src="https://user-images.githubusercontent.com/37180024/50382589-44395a00-06b4-11e9-96b8-cc51dfd3a9ea.png" alt="Рисунок 6. Таблица, предназначенная для хранения данных об областях применения определенного языка программирования">
<p>Рисунок 6. Модель структуры базы данных</p>
